#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin >> n;
    for (int i = 1; i < n; i++)
        if (a[i] < a[i+1])
    {
        v[i].push_back(i+1);
    } else
        if (a)
    return 0;
}
