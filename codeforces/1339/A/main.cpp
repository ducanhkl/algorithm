#include <bits/stdc++.h>

using namespace std;

int test = 0, x = 0;

int main()
{
    ios_base::sync_with_stdio(false);
    cin >> test;
    while(test--)
    {
        cin >> x;
        cout << x << endl;
    }
    return 0;
}
